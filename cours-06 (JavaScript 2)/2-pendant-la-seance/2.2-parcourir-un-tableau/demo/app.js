// 1. Définir un tableau
var produits = [
	"chemise",
	"jeans",
	"chapeau",
	"bas",
	"coton ouaté"
];

console.log(produits);

// 2. Accéder à un seul élément du tableau, grâce à son index
var monIndex = 1;
console.log(produits[monIndex]);

// 3. Déterminer la quantité d'éléments dans le tableau
console.log(produits.length);

// 4. Écrire une boucle dans sa forme la plus simple
// 6. On définit la condition de manière dynamique
for (var i = 0; i < produits.length; i = i + 1) {

	// 5. On utilise le compteur i comme index
	console.log(produits[i]);
}








